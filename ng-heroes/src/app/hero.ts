//this is OOP JS object. Not an object literal. 
//This behaves the same way a component behaves. 
//you can also use a service the same way as a component. 
export class Hero {
    //treat this as a dataset.
    //every hero will be of this structure. 
    id: number;
    name: string; 
}